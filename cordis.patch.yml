# The bundle is safe to install but inert until its Web-profile configuration
# explicitly sets enabled: true. Use `dsh-one-gateway setup` to generate an enabled
# user-instance entry after confirmation.
- insert:
    - id: dsh-one-gateway
      name: dsh-one-gateway
      config:
        enabled: false
