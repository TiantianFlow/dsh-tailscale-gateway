# The bundle is safe to install but inert until its Web-profile configuration
# explicitly sets enabled: true. Use `dsh-gateway setup` to generate an enabled
# user-instance entry after confirmation.
- insert:
    - id: dsh-gateway
      name: dsh-gateway
      config:
        enabled: false
